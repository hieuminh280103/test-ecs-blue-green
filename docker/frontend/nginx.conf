server {
    listen 80;
    location /health {
        add_header Content-Type text/plain;
        return 200 'ok';
    }
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
